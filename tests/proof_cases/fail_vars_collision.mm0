delimiter $ ( ) $;
provable sort wff;

term imp (a b: wff): wff;
infixr imp: $->$ prec 25;

-- `imp` is already a term name; claiming it as @vars should fail
--| @vars imp
sort nat;

axiom ax1 (a b: wff): $ a -> b -> a $;
theorem fail_test (a: wff): $ a -> a -> a $;
