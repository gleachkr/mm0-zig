delimiter $ ( ) $;
provable sort wff;
sort obj;

term all {x: obj} (p: wff x): wff;
prefix all: $A.$ prec 41;
term eq (a b: obj): wff;
infixl eq: $=$ prec 35;

--| @dummy x
axiom all_refl {x: obj}: $ A. x (x = x) $;

theorem alpha_only_line {y: obj}: $ A. y (y = y) $;
